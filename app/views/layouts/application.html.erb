<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
          <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"> 

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Im a Model" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>

<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="/">iModel</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
     <% if admin_signed_in? %>
      <li class="has-dropdown">
       <a href="#" class="navbar-link"><%= "Hi " + current_admin.name%></a>
        <ul class="dropdown">
          <li><%= link_to "Add pics", new_post_path%></li>
          <li><%= link_to "Gallery", posts_path%></li>
          <li><%= link_to "Edit profile", edit_admin_registration_path, class: "navbar-link" %></li>
          <li><%= link_to "Sign Out", destroy_admin_session_path, method: :delete, class: "navbar-link" %></li>
        </ul>
      </li>
      <% else %>
      <li><%= link_to "Gallery", posts_path%></li>
      <% end %>
    </ul>

    <!-- Left Nav Section -->
  <!--   <ul class="left">
      <li><a href="#">Left Nav Button</a></li>
    </ul> -->
  </section>
</nav>


<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>