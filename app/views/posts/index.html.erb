



    <% @posts.each do |post| %>

 <div data-alt="img03" data-description="<h3>Sky high</h3>" data-max-width="1800" data-max-height="1350">
                <div data-src="<%= post.picture.url %>" data-min-width="700"></div>
                <div data-src="<%= post.picture.url(:large) %>" data-min-width="300"></div>
                <div data-src="<%= post.picture.url(:medium) %>" data-min-width="200"></div>
                <div data-src="<%= post.picture.url(:small) %>" data-min-width="140"></div>
                  <img src="<%= post.picture.url(:medium) %>" alt="img03"/>
              </div>


    <% end %>

    <%= link_to "new post", new_post_path %>



    